---
phase: 01-release-packaging
plan: 03
type: execute
wave: 2
depends_on: [01-01]
autonomous: true
requirements:

  - QA-01
  - QA-02
  - QA-03
  - QA-04

must_haves:
  truths:

    - .pre-commit-config.yaml exists with shellcheck, shfmt, and standard hooks pinned to exact versions
    - pre-commit run --all-files exits 0 on the current codebase
    - If shfmt reformats terraform-credentials-keychain or install.sh, the reformatted version is committed
  artifacts:

    - "path: ".pre-commit-config.yaml"
    - "path: ".pre-commit-config.yaml"
  key_links:

    - "from: ".pre-commit-config.yaml"
    - "from: ".pre-commit-config.yaml"

files_modified:

  - .pre-commit-config.yaml
  - terraform-credentials-keychain
  - install.sh

---

<objective>
Create .pre-commit-config.yaml with shellcheck, shfmt, and standard pre-commit hooks pinned to verified versions, then run all hooks on the codebase and fix any formatting issues so QA-04 passes clean.

Purpose: Establishes quality gates so future contributions can't introduce shell script bugs or inconsistent formatting.
Output: .pre-commit-config.yaml; any shfmt-driven formatting fixes applied to terraform-credentials-keychain and/or install.sh.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-release-packaging/01-RESEARCH.md
@.planning/phases/01-release-packaging/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create .pre-commit-config.yaml (QA-01, QA-02, QA-03)</name>
  <files>.pre-commit-config.yaml</files>
  <read_first>.planning/phases/01-release-packaging/01-RESEARCH.md</read_first>
  <action>
Create .pre-commit-config.yaml in the repo root with exactly this content:

```yaml
repos:

  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v6.0.0
    hooks:

      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: check-yaml
      - id: mixed-line-ending

  - repo: https://github.com/koalaman/shellcheck-precommit
    rev: v0.11.0
    hooks:

      - id: shellcheck

  - repo: https://github.com/scop/pre-commit-shfmt
    rev: v3.13.0-1
    hooks:

      - id: shfmt

```

These are the exact revs verified in the research (2026-03-24). Do NOT use the `shfmt-src` hook — it requires Go installed. Use `shfmt` (prebuilt binary hook) instead.
  </action>
  <verify>
    <automated>grep -q "koalaman/shellcheck-precommit" .pre-commit-config.yaml && grep -q "v0.11.0" .pre-commit-config.yaml && grep -q "scop/pre-commit-shfmt" .pre-commit-config.yaml && grep -q "v3.13.0-1" .pre-commit-config.yaml && grep -q "pre-commit/pre-commit-hooks" .pre-commit-config.yaml && grep -q "v6.0.0" .pre-commit-config.yaml && python3 -c "import yaml; yaml.safe_load(open('.pre-commit-config.yaml'))" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>

    - .pre-commit-config.yaml exists
    - Contains `koalaman/shellcheck-precommit` at rev `v0.11.0`
    - Contains `scop/pre-commit-shfmt` at rev `v3.13.0-1` with hook id `shfmt` (NOT `shfmt-src`)
    - Contains `pre-commit/pre-commit-hooks` at rev `v6.0.0`
    - Contains hooks: `trailing-whitespace`, `end-of-file-fixer`, `check-yaml`, `mixed-line-ending`
    - File is valid YAML (python3 -c "import yaml; yaml.safe_load(open('.pre-commit-config.yaml'))" exits 0)
  </acceptance_criteria>
  <done>.pre-commit-config.yaml created with all three hook repos pinned to exact verified versions.</done>
</task>

<task type="auto">
  <name>Task 2: Run pre-commit --all-files and fix any issues (QA-04)</name>
  <files>terraform-credentials-keychain, install.sh</files>
  <read_first>terraform-credentials-keychain, install.sh, .pre-commit-config.yaml</read_first>
  <action>
Install pre-commit hooks and run all hooks on the full codebase. Fix any issues that are found.

Step 1 — Install hooks:

```bash
pre-commit install
```

Step 2 — Run all hooks and capture output:

```bash
pre-commit run --all-files
```

Step 3 — If shfmt modifies `terraform-credentials-keychain` or `install.sh`:

- Read the diff to understand what changed
- Verify the changes are purely cosmetic (whitespace, indentation, redirect spacing) — NOT logic changes
- Accept the shfmt changes (they are correct per the project's own quality gate)
- The files will be modified in-place by pre-commit; no additional action needed beyond verifying logic is intact

Step 4 — If shellcheck reports errors:

- Fix each SC-code error in the relevant file
- Re-run `pre-commit run --all-files` until it passes

Step 5 — If `end-of-file-fixer` or `trailing-whitespace` hooks modify files, those changes are correct — accept them.

The goal is `pre-commit run --all-files` exiting 0. Do not disable or exclude hooks to make them pass — fix the underlying issues.

Important: shfmt reformatting is EXPECTED and acceptable per research Pitfall 2. The key check is that logic (exit codes, conditionals, case statement flow) is unchanged after reformatting.
  </action>
  <verify>
    <automated>pre-commit run --all-files && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>

    - `pre-commit run --all-files` exits 0
    - No hooks report "Failed" status
    - If shfmt modified files: the modifications are whitespace/formatting only, not logic changes
    - shellcheck reports zero errors on terraform-credentials-keychain and install.sh
  </acceptance_criteria>
  <done>pre-commit run --all-files exits 0. All shellcheck and shfmt hooks pass on the current codebase.</done>
</task>

</tasks>

<verification>

- `python3 -c "import yaml; yaml.safe_load(open('.pre-commit-config.yaml'))"` exits 0
- `pre-commit run --all-files` exits 0
- `grep -q "v0.11.0" .pre-commit-config.yaml` exits 0
- `grep -q "v3.13.0-1" .pre-commit-config.yaml` exits 0

</verification>

<success_criteria>

1. .pre-commit-config.yaml exists with shellcheck v0.11.0, shfmt v3.13.0-1, and pre-commit-hooks v6.0.0
2. `pre-commit run --all-files` exits 0 — all hooks pass on the current codebase
3. Any shfmt formatting changes are applied and accepted (logic unchanged)

</success_criteria>

<output>
After completion, create `.planning/phases/01-release-packaging/01-03-SUMMARY.md`
</output>
