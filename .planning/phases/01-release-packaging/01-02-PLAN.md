---
phase: 01-release-packaging
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - DOC-01
  - DOC-02
  - DOC-03
  - DOC-04

must_haves:
  truths:
    - "README.md identifies this as a fork with a link to the upstream repo"
    - "README.md does not contain the bendrucker deprecation notice"
    - "README.md shows install.sh as the recommended installation method"
    - "README.md includes manual installation steps as a fallback"
    - "README.md lists jq and macOS as dependencies"
    - "README.md includes the credentials registry.terraform.io empty block advice"
  artifacts:
    - path: "README.md"
      provides: "User-facing documentation for the fork"
      contains: "alisdair/terraform-credentials-keychain"
  key_links:
    - from: "README.md Installation section"
      to: "install.sh"
      via: "bash install.sh or ./install.sh command in code block"
      pattern: "install\\.sh"
    - from: "README.md Manual Installation section"
      to: "~/.terraformrc stanza"
      via: "credentials_helper \"keychain\" {} code block"
      pattern: "credentials_helper"
---

<objective>
Rewrite README.md to reflect the fork identity, document install.sh as the recommended install path, provide manual fallback instructions, list dependencies, and include the upstream credentials empty-block advice.

Purpose: A new user reading the README knows this is an active maintained fork, can install in one command, and has all the information needed to configure Terraform correctly.
Output: Updated README.md replacing the deprecated upstream content.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-release-packaging/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite README.md (DOC-01, DOC-02, DOC-03, DOC-04)</name>
  <files>README.md</files>
  <read_first>README.md, .planning/phases/01-release-packaging/01-RESEARCH.md</read_first>
  <action>
Replace the entire contents of README.md. Do not carry over any content from the current README — it contains an upstream deprecation notice that must be removed entirely.

The new README.md content (write this verbatim, including all fenced code blocks):

    # terraform-credentials-keychain

    A macOS Keychain-backed Terraform credentials helper. Stores tokens for
    Terraform Enterprise / HCP Terraform hostnames in the macOS Keychain instead
    of plain-text `~/.terraform.d/credentials.tfrc.json`.

    **Fork of [alisdair/terraform-credentials-keychain](https://github.com/alisdair/terraform-credentials-keychain)
    (original by Alisdair McDiarmid, MIT licensed). This fork is actively maintained.**

    ## Requirements

    - macOS (uses the built-in `security` CLI)
    - [`jq`](https://jqlang.org/) — required for the `store` command; install via `brew install jq`
    - Terraform CLI

    ## Installation

    ### Recommended: install.sh

    ```bash
    git clone https://github.com/langburd/terraform-credentials-keychain.git
    cd terraform-credentials-keychain
    bash install.sh
    ```

    This copies the script to `~/.terraform.d/plugins/` and patches `~/.terraformrc` to enable the
    credentials helper automatically. It is safe to run more than once.

    ### Manual Installation

    1. Copy `terraform-credentials-keychain` to `~/.terraform.d/plugins/`
    2. Make it executable: `chmod +x ~/.terraform.d/plugins/terraform-credentials-keychain`
    3. Add the following block to `~/.terraformrc`:

       ```hcl
       credentials_helper "keychain" {}
       ```

    4. Optionally, add an empty credentials block for the public registry to prevent the helper
       from being invoked for provider/module installs:

       ```hcl
       credentials "registry.terraform.io" {}
       ```

    ## Usage

    After installation, use standard Terraform commands to manage credentials:

    - `terraform login <hostname>` — store a token in the Keychain
    - `terraform logout <hostname>` — remove a stored token

    ## License

    MIT — see [LICENSE.md](LICENSE.md).
    Original work Copyright (c) 2020 Alisdair McDiarmid. Fork Copyright (c) 2026 Avi Langburd.

Requirements each section satisfies:
- DOC-01: Fork identification + upstream link in the opening paragraph (alisdair/terraform-credentials-keychain)
- DOC-02: install.sh documented under "Recommended: install.sh" sub-heading
- DOC-03: "Manual Installation" section with numbered steps
- DOC-04: Requirements section listing macOS and jq explicitly
- Key decision: `credentials "registry.terraform.io" {}` empty block included in step 4 of manual install
- The "bendrucker" deprecation notice is NOT present in the new content — it must not be added
  </action>
  <verify>
    <automated>grep -q "alisdair/terraform-credentials-keychain" README.md && grep -q "install.sh" README.md && grep -q "Manual Installation" README.md && grep -q "jq" README.md && grep -q 'credentials "registry.terraform.io"' README.md && ! grep -q "bendrucker" README.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - README.md contains "alisdair/terraform-credentials-keychain" (fork attribution with upstream link)
    - README.md does NOT contain "bendrucker" anywhere
    - README.md contains "install.sh" (recommended installation method)
    - README.md contains "Manual Installation" section heading
    - README.md contains "jq" in the Requirements section
    - README.md contains `credentials "registry.terraform.io"` (empty block advice)
    - README.md contains `credentials_helper "keychain" {}` (terraformrc stanza)
  </acceptance_criteria>
  <done>README.md replaced with fork-aware content documenting install.sh, manual steps, dependencies, and the registry.terraform.io empty-block advice. No deprecation notice present.</done>
</task>

</tasks>

<verification>
- `grep -q "alisdair/terraform-credentials-keychain" README.md` exits 0
- `grep -q "bendrucker" README.md` exits 1 (not found — deprecation notice gone)
- `grep -q "install.sh" README.md` exits 0
- `grep -q "Manual Installation" README.md` exits 0
- `grep -q "jq" README.md` exits 0
</verification>

<success_criteria>
README.md identifies the fork, documents install.sh as recommended installation, provides manual fallback steps, lists macOS and jq as dependencies, and includes the registry.terraform.io empty block advice. No deprecation notice remains.
</success_criteria>

<output>
After completion, create `.planning/phases/01-release-packaging/01-02-SUMMARY.md`
</output>
